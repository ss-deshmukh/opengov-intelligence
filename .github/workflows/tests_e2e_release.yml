name: Release e2e scenarios

permissions:
  contents: read

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  e2e-live:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install 3.12

      - name: Run E2E tests (live)
        env:
          OSCAT_OPENAI_API_KEY: ${{ secrets.OSCAT_OPENAI_API_KEY }}
          OSCAT_PLANNING_PROVIDER: openai
          OSCAT_CODING_PROVIDER: openai
          OSCAT_PLANNING_MODEL: gpt-4.1-mini
          OSCAT_CODING_MODEL: gpt-4.1-mini
        run: uv run --extra dev pytest tests/e2e/ --live -v